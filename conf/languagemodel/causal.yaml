# causal language model is a language model that only uses the left context to predict the next token.
name              : 'causal' # name of the configuration
masked_lm         : False   # Do not use masked language model
is_causal         : True    # causal attention
model_ckpt        : 'model/checkpoints/causal_model_checkpoint.pth' # model checkpoint